name: Node CI
on: 
  - push
  - pull_request
jobs:
  build:
    runs-on: ubuntu-latest
  strategy:
    matrix:
      node-version: [16.x]
  steps:
      uses: actions/checkout@v2
    - name: Use Node.js
      uses: actions/setup-node@v1
    - name: run linter
      run: make lint